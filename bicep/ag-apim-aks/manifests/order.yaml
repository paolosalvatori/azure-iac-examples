apiVersion: v1
kind: Service
metadata:
  namespace: apis
  name: order-svc
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: order-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: apis
  name: order-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-api
  template:
    metadata:
      labels:
        app: order-api
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: debug
        resources:
          limits:
            cpu: "500m"
            memory: "64Mi"
          requests:
            cpu: "250m"
            memory: "32Mi"
        image: acr5xaacqhgxhjqa.azurecr.io/order:dev-0.1.0
        ports:
        - containerPort: 8080
          name: http
      