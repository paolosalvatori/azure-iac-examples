apiVersion: v1
kind: Service
metadata:
  namespace: apis
  name: product-svc
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8081
  selector:
    app: product-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: apis
  name: product-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-api
  template:
    metadata:
      labels:
        app: product-api
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: debug
        resources:
          limits:
            cpu: "500m"
            memory: "64Mi"
          requests:
            cpu: "250m"
            memory: "32Mi"
        image: acr5xaacqhgxhjqa.azurecr.io/product:dev-0.1.0
        ports:
        - containerPort: 8081
          name: http
      